## Hello what is up guys

git init => version control start under that folder on computer

git add . => stage everything till yet inside the folder

git commit -m "first" => checkpoint creation

git branch -M main => master/main branch mein

git remote add origin <link> => where exactly to send this code

git push -u origin main => push this to mentioned origin(link) and usme main branch me
